# 루트 .env 로 복사(gitignore). 실제 비밀값은 커밋하지 않는다.
# 자세한 내용: docs/forUser/SettingGuide/02-cursor-credentials.md
#
# harness_cli_loop.py 는 실행 시 이 파일을 읽어 환경 변수에 넣는다(이미 설정된 키는 덮어쓰지 않음).

# 헤드리스 CLI(agent 자식 프로세스가 상속)
CURSOR_API_KEY=replace_me
